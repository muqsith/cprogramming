#include <stdio.h>

int main()
{
    int num = 1;
    for (int i = 1; i < ((sizeof(int) * 8)); i++)
    {
        num = num << 1;
        printf("num = %d, i = %d\n", num, i);
        int x;
        x==2;
    }
}
